{
  "additionalProperties": false,
  "description": "Represents a single surgery entry from surgeries.csv.\n\nFields:\n    surgery_id: Unique identifier of the surgery.\n    start_time: Surgery start time (ISO-8601, expected UTC unless Config.timezone provided).\n    end_time:   Surgery end time   (ISO-8601, expected UTC unless Config.timezone provided).\n    duration:   Optional duration in hours; may be computed as (end_time – start_time).\n    room_hint:  Optional preferred operating-room label.",
  "properties": {
    "surgery_id": {
      "description": "Unique identifier",
      "title": "Surgery Id",
      "type": "string"
    },
    "start_time": {
      "description": "Surgery start time (ISO-8601)",
      "format": "date-time",
      "title": "Start Time",
      "type": "string"
    },
    "end_time": {
      "description": "Surgery end time (ISO-8601)",
      "format": "date-time",
      "title": "End Time",
      "type": "string"
    },
    "duration": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Duration in hours; optional if computable from timestamps",
      "title": "Duration"
    },
    "room_hint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Preferred operating room label (optional)",
      "title": "Room Hint"
    }
  },
  "required": [
    "surgery_id",
    "start_time",
    "end_time"
  ],
  "title": "Surgery",
  "type": "object"
}
